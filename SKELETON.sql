SELECT title, rental_rate
FROM film
WHERE rental_rate = .99
;

#Quiz Question3
#What is the name of the actor/actress with the actor_id = 50?
SELECT first_name, last_name
FROM actor
WHERE actor_id = 50
;